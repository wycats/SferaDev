# READ-ONLY: Use 'exo' CLI to modify this file.
# Implementation Plan

[phase]
id = "01kgt5p8kcsy08hdnevegysp3x"
title = "Remove 1.5x Multiplier System"
rfcs = ["00054"]

# ============================================================================
# PREPARE REPORT (Validated 2026-02-06)
# ============================================================================
#
# ZOMBIE DEFINITION: Code that executes but is broken AND superseded by newer
# mechanisms. We accept gaps and plan to address them if needed.
#
# WHAT WE'RE REMOVING:
# - learnedTokenTotal field (L129-132)
# - hashConversation() function (L645-660) 
# - 1.5x multiplier logic in provideTokenCount() (L689-707)
# - Zombie portion of error handler (L577-590)
# - Success path clearing learnedTokenTotal (L555-560)
#
# WHAT WE'RE KEEPING:
# - hashMessage() - used for chatId generation (L338)
# - modelInfoChangeEmitter.fire() at L156, L859 - legitimate notifications
# - extractTokenCountFromError() - used in openresponses-chat.ts AND status bar
# - Status bar error display (L593-600) - shows user why request failed
# - Agent error marking (L604-606)
#
# GAP ACKNOWLEDGED:
# Rolling correction only updates on successful requests. On "too long" error,
# nothing adjusts estimates. The zombie code handled this (badly). Goal 2 will
# design proper error recovery.
#
# ============================================================================

[plan]

[[plan.goals]]
id = "remove-zombie-multiplier"

# Execution order matters - remove usages before definitions
[[plan.goals.tasks]]
id = "remove-1.5x-branch"
title = "Remove 1.5x multiplier branch in provideTokenCount() (L689-707)"
status = "pending"

[[plan.goals.tasks]]
id = "remove-success-clear"
title = "Remove success path that clears learnedTokenTotal (L555-560)"
status = "pending"

[[plan.goals.tasks]]
id = "surgical-error-handler"
title = "Surgically remove zombie portion of error handler (L577-590), keeping status bar display"
status = "pending"

[[plan.goals.tasks]]
id = "remove-hash-conversation"
title = "Remove hashConversation() function (L645-660)"
status = "pending"

[[plan.goals.tasks]]
id = "remove-field"
title = "Remove learnedTokenTotal field and inline type (L129-132)"
status = "pending"

[[plan.goals]]
id = "design-error-recovery"

[[plan.goals.tasks]]
id = "document-gap"
title = "Document the error recovery gap in RFC 00054"
status = "pending"

[[plan.goals.tasks]]
id = "propose-options"
title = "Propose options: (1) do nothing, (2) clear correction on error, (3) feed error to rolling correction"
status = "pending"

[verification]
automated = ["pnpm test", "pnpm typecheck"]
manual = ["Verify status bar still shows token info on 'too long' errors"]
