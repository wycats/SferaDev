# READ-ONLY: Use 'exo' CLI to modify this file.
# Implementation Plan

[phase]
id = "01kgqrm6fjqy1xb0te56ks9r8r"
title = "Phase 2: provideTokenCount Integration (RFC 047)"
rfcs = ["00047"]

[plan]

[[plan.goals]]
id = "apply-rolling-correction"

[[plan.goals.tasks]]
id = "get-adjustment"
title = "Add getAdjustment() method to HybridTokenEstimator"
status = "pending"

[[plan.goals.tasks]]
id = "first-message-detection"
title = "Detect first message in turn via wouldStartNewSequence() in estimateMessage()"
status = "pending"

[[plan.goals.tasks]]
id = "apply-adjustment"
title = "Apply adjustment to first message estimate and return corrected value"
status = "pending"

[[plan.goals.tasks]]
id = "pass-context"
title = "Pass modelFamily and conversationId to estimateMessage() from provideTokenCount()"
status = "pending"
# Goals contain tasks. Use 'exo-add-task' to add tasks to goals.


[verification]
automated = ["Run scripts/verify-phase.sh"]
manual = []
